# VLMProjects — unified pip requirements (Apple Silicon MacBook / MPS)
# Runs ALL projects in this repo. Use this if you prefer a plain venv over conda:
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python check_mps.py        # expect "MPS available: True"
# See environment.yml for the conda path, the macOS libomp gotcha, and the
# CARLA caveat (carla/leaderboard are Linux-only and intentionally omitted).

# core deep learning
torch==2.12.0
torchvision==0.27.0

# numerics / imaging
numpy==2.4.6
scipy==1.17.1
matplotlib==3.10.9
einops==0.8.2
opencv-python-headless==4.11.0.86
pillow==12.2.0
imageio==2.37.3
scikit-image==0.26.0

# nuScenes data + geometry
nuscenes-devkit==1.2.0
pyquaternion==0.9.9

# Argoverse 2 motion forecasting (QCNet)
av2==0.3.6
pandas==3.0.3
pyarrow==24.0.0

# model utilities / training / logging
efficientnet_pytorch==0.7.1
timm==1.0.27
tensorboardX==2.6.5
tqdm==4.67.3
pyyaml==6.0.3
fire==0.7.1
setuptools

# optional: only for the Lyft eval/train scripts in simple_bev
# lyft-dataset-sdk==0.0.8
