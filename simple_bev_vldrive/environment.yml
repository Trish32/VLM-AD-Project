name: simple_bev_vldrive
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy
  - scipy
  - matplotlib
  - pip:
      - torch>=2.2.0
      - torchvision>=0.16.0
      - einops
      - opencv-python-headless
      - tqdm
      - pyyaml
      - tensorboard
      - timm
