"""Runnable experiments, ablations and the visualiser.

Nothing in the package imports from here. Each module is a `python -m` entry
point that reproduces one table in EXPERIMENT.md, which is why they are kept
rather than deleted after the number was recorded.
"""
