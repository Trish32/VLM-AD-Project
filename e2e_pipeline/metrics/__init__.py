"""Scoring a rollout.

`metrics` is the five families (safety, route, comfort, prediction, latency).
`divergence_metrics` exists because the first set measured deviation from the
recording rather than driving quality -- see EXPERIMENT.md Sec 19 and Sec 25.
"""
